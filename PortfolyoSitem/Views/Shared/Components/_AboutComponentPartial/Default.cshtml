@using PortfolyoSitem.Data
@model AboutTable

<section id="about"
    class="min-h-screen flex flex-col justify-center px-8 md:px-20 py-20 border-t border-white/5 reveal-section">
    <div class="max-w-4xl mx-auto w-full">
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-bold mb-4 relative inline-block">
                <span
                    class="absolute -inset-4 bg-gradient-to-r from-red-600 via-orange-500 to-yellow-400 opacity-20 blur-xl rounded-full"></span>
                <span class="relative text-white uppercase tracking-wider">Hakkımda</span>
            </h2>
            <p class="text-muted max-w-2xl mx-auto">
                @Model.Title
            </p>
        </div>
        <div class="flex justify-center relative max-w-4xl w-full mx-auto">
            <div class="absolute inset-0 rounded-2xl" 
                 style="padding: 2px; background: linear-gradient(to right, rgba(220, 38, 38, 0.4), rgba(249, 115, 22, 0.4), rgba(250, 204, 21, 0.4)); -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0); -webkit-mask-composite: xor; mask-composite: exclude; pointer-events: none; filter: drop-shadow(0 0 15px rgba(255,87,34,0.15));">
            </div>
            <div class="space-y-6 text-lg text-muted font-body p-8 rounded-2xl h-full w-full bg-transparent relative z-10">
                <p class="text-center">
                    @Model.Description
                </p>
            </div>
        </div>
    </div>
</section>
